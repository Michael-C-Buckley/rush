tmpdir=${TMPDIR:-/tmp}
i=0
while test "$i" -lt 32; do
  : > "$tmpdir/.rush-heredoc-$$-$i.tmp"
  i=$((i + 1))
done
cat <<EOF &
data
EOF
echo spawn=$?
wait
i=0
while test "$i" -lt 32; do
  rm -f "$tmpdir/.rush-heredoc-$$-$i.tmp"
  i=$((i + 1))
done
