set -o | grep '^emacs[[:space:]]*on' >/dev/null && echo emacs:default
set -o vi
set -o | grep '^emacs[[:space:]]*off' >/dev/null && set -o | grep '^vi[[:space:]]*on' >/dev/null && echo vi:on-emacs:off
set -o emacs
set -o | grep '^emacs[[:space:]]*on' >/dev/null && set -o | grep '^vi[[:space:]]*off' >/dev/null && echo emacs:on-vi:off
set +o emacs
set -o | grep '^emacs[[:space:]]*off' >/dev/null && set -o | grep '^vi[[:space:]]*off' >/dev/null && echo emacs:off
set -o vi
set +o vi
set -o | grep '^emacs[[:space:]]*on' >/dev/null && set -o | grep '^vi[[:space:]]*off' >/dev/null && echo vi:disable-default
