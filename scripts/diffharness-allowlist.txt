# Known filed bugs suppressed from diffharness findings.
# Entry formats:
#   <16-hex fingerprint>     pins one exact minimized script
#   match:<substring>        matched against the minimized script (one line)
# Remove the entry when the referenced task is fixed to re-enable detection.

# #663 signal during in-process subshell/cmdsub never fires parent trap
match:; (kill -

# #694 cd .. after symlink: physical chdir desyncs PWD
match:cd l1; pwd; cd ..

# #697 case inside ( ) subshell fails to parse (case may appear after other
# statements inside the parens, so match the keyword in paren context)
match:(case
match:; case
match:&& case
match:|| case
52b426712dc93d83
e47a8363028ee0fd

# #698 compound body span mis-slice (while/until/if bodies with nested
# same-keyword constructs after a non-first statement) -> ParseError abort
match:; for
match:(for
match:{ for
fa3e6348b2af70ed

# #662 use-after-free / lost output when a function redefines itself
match:fn1() { fn1()
match:fn2() { fn2()

# #699 whole-script parse aborts before executing earlier complete commands
# (no stable substring; late-parse-error cases are seed-specific. Most were
# generator-induced before heredoc nesting was fixed; revisit if they recur.)

# #706 backslash-in-cmdsub-in-double-quotes divergence (rush+ksh verbatim
# vs dash/bash/zsh/yash collapsing)
match:printf '%s\\n'
match:then case
match:then for
match:(if
