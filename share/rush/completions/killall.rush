__rush_complete_killall_process_names() {
  ps -A -o comm= 2>/dev/null | sort -u | while read command; do
    test -n "$command" || continue
    name=${command##*/}
    test -n "$name" || continue
    rush_complete candidate "$name" --kind plain --description "$command"
  done
}
