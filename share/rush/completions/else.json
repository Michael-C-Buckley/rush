{
  "$schema": "https://rush.horse/completion/schema/v1.schema.json",
  "manifestVersion": 1,
  "command": {
    "name": "else",
    "description": "execute a command when the previous condition failed",
    "arguments": {
      "states": [
        {
          "name": "command",
          "provider": "builtin.executables",
          "index": 0,
          "description": "command to execute"
        }
      ]
    },
    "providers": {
      "builtin.executables": {
        "builtin": "executables"
      }
    }
  }
}
