{
  "$schema": "https://rush.horse/completion/schema/v1.schema.json",
  "manifestVersion": 1,
  "command": {
    "name": "elif",
    "description": "execute a command when the previous condition succeeded",
    "arguments": {
      "states": [
        {
          "name": "command",
          "provider": "builtin.executables",
          "index": 0,
          "description": "command to execute"
        }
      ]
    },
    "providers": {
      "builtin.executables": {
        "builtin": "executables"
      }
    }
  }
}
